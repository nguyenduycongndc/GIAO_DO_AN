@using Data.DB
@using APIProject.Resource

    @{
    <option>@Resources.AllDistricts</option> 
        foreach (var d in (List<District>)ViewBag.listDistrict)
        {
            <option value="@d.Code">@d.Name</option>
        }
    }


